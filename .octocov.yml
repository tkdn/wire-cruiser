coverage:
  paths:
    - coverage.out

comment:
  if: is_pull_request

summary:
  if: true

report:
  if: is_default_branch
  datastores:
    - artifact://${GITHUB_REPOSITORY}

diff:
  if: is_pull_request
  datastores:
    - artifact://${GITHUB_REPOSITORY}
